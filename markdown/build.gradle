archivesBaseName = 'collap-std-markdown'
version = '0.1.1'

dependencies {
    compile 'org.pegdown:pegdown:1.4.2'
}

jar {
    manifest {
        attributes 'Implementation-Title': archivesBaseName, 'Implementation-Version': version
    }
}

