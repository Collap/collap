archivesBaseName = 'collap-std-post'
version = '0.1.1'

dependencies {
    compile project (':user')
}

jar {
    manifest {
        attributes 'Implementation-Title': archivesBaseName, 'Implementation-Version': version
    }
}

